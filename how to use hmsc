Hide My Source Code

How to use
Lnux
built-in web server
php -d zend_extension=/path/extension_name.so -S localhost:8080

cli
php -d zend_extension=/path/extension_name.so -m


Windows
built-in web server
php -d zend_extension=/path/extension_name.so -S localhost:8080

cli 
php -d zend_extension=/path/extension_name.dll -m


enable php extension from php.ini
; Directory in which the loadable extensions (modules) reside.
; https://php.net/extension-dir
;extension_dir = "./"
; On windows:
;extension_dir = "ext"

zend_extension=extension_name(.so / .dll)

with nginx
service nginx restart

with apache
apache2ctl restart

with php-fpm
service php-fpm restart
specific version
service php7.X-fpm restart
service php8.X-fpm restart
